name: crewmanpower
description: "A new Crewmanpower."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter
  shared_preferences: ^2.2.0

  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  provider: ^6.0.5
  url_launcher: ^6.3.2
  file_picker: ^11.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  

flutter:
  uses-material-design: true

  assets:
    - assets/images/